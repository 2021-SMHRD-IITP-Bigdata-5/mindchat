const router = require("react-router-dom/es/Router");
const express = require("express");
const app = express();

app.use('/api/product', require('./routes/product'));

router
app.post('/api/product/image',ko)
